{
  "name": "planne-landing-page",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page estatica da Planne em HTML, CSS e JavaScript puro.",
  "scripts": {
    "build": "node scripts/generate-occasion-pages.mjs && node scripts/normalize-pt-home.mjs",
    "validate:occasions": "node scripts/validate-occasion-pages.mjs",
    "deploy": "firebase deploy"
  }
}
